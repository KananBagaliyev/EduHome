@model IEnumerable<Course>

@foreach (Course course in Model)
{
    <li class="list-group-item" style="padding-bottom:13px">
        <a asp-controller="Course" asp-action="Detail" asp-route-id="@course.Id">@course.Name</a>
    </li>
}
